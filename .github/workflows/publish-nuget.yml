name: Build and Publish

on:
  pull_request:
    branches: [ main ]
  push:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest
    if: github.event_name == 'pull_request'

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Setup .NET
      uses: actions/setup-dotnet@v4
      with:
        dotnet-version: '10.0.x'

    - name: Restore dependencies
      run: |
        dotnet restore src/CodeTailor.Domain/CodeTailor.Domain.csproj
        dotnet restore src/CodeTailor.Application/CodeTailor.Application.csproj
        dotnet restore src/CodeTailor.Infrastructure/CodeTailor.Infrastructure.csproj
        dotnet restore src/CodeTailor.Presentation/CodeTailor.Presentation.csproj
        dotnet restore src/CodeTailor.Testing/CodeTailor.Testing.csproj

    - name: Build
      run: |
        dotnet build src/CodeTailor.Domain/CodeTailor.Domain.csproj --configuration Release --no-restore
        dotnet build src/CodeTailor.Application/CodeTailor.Application.csproj --configuration Release --no-restore
        dotnet build src/CodeTailor.Infrastructure/CodeTailor.Infrastructure.csproj --configuration Release --no-restore
        dotnet build src/CodeTailor.Presentation/CodeTailor.Presentation.csproj --configuration Release --no-restore
        dotnet build src/CodeTailor.Testing/CodeTailor.Testing.csproj --configuration Release --no-restore

  publish:
    runs-on: ubuntu-latest
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'

    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      with:
        fetch-depth: 0

    - name: Setup .NET
      uses: actions/setup-dotnet@v4
      with:
        dotnet-version: '10.0.x'

    - name: Generate version number
      id: version
      run: |
        # Use simple incremental versioning: 1.0.build_number
        VERSION="1.0.${{ github.run_number }}"
        echo "VERSION=$VERSION" >> $GITHUB_OUTPUT
        echo "Publishing version: $VERSION"

    - name: Update project versions
      run: |
        # Update all .csproj files with new version
        find src -name "*.csproj" -type f -exec sed -i "s/<Version>.*<\/Version>/<Version>${{ steps.version.outputs.VERSION }}<\/Version>/g" {} \;

    - name: Restore dependencies
      run: |
        dotnet restore src/CodeTailor.Domain/CodeTailor.Domain.csproj
        dotnet restore src/CodeTailor.Application/CodeTailor.Application.csproj
        dotnet restore src/CodeTailor.Infrastructure/CodeTailor.Infrastructure.csproj
        dotnet restore src/CodeTailor.Presentation/CodeTailor.Presentation.csproj
        dotnet restore src/CodeTailor.Testing/CodeTailor.Testing.csproj

    - name: Build
      run: |
        dotnet build src/CodeTailor.Domain/CodeTailor.Domain.csproj --configuration Release --no-restore
        dotnet build src/CodeTailor.Application/CodeTailor.Application.csproj --configuration Release --no-restore
        dotnet build src/CodeTailor.Infrastructure/CodeTailor.Infrastructure.csproj --configuration Release --no-restore
        dotnet build src/CodeTailor.Presentation/CodeTailor.Presentation.csproj --configuration Release --no-restore
        dotnet build src/CodeTailor.Testing/CodeTailor.Testing.csproj --configuration Release --no-restore

    - name: Pack
      run: |
        dotnet pack src/CodeTailor.Domain/CodeTailor.Domain.csproj --configuration Release --no-build --output ./nupkg
        dotnet pack src/CodeTailor.Application/CodeTailor.Application.csproj --configuration Release --no-build --output ./nupkg
        dotnet pack src/CodeTailor.Infrastructure/CodeTailor.Infrastructure.csproj --configuration Release --no-build --output ./nupkg
        dotnet pack src/CodeTailor.Presentation/CodeTailor.Presentation.csproj --configuration Release --no-build --output ./nupkg
        dotnet pack src/CodeTailor.Testing/CodeTailor.Testing.csproj --configuration Release --no-build --output ./nupkg

    - name: Publish to NuGet
      run: dotnet nuget push ./nupkg/*.nupkg --api-key ${{ secrets.NUGET_API_KEY }} --source https://api.nuget.org/v3/index.json --skip-duplicate

    - name: Create GitHub Release
      uses: softprops/action-gh-release@v1
      with:
        tag_name: v${{ steps.version.outputs.VERSION }}
        name: Release v${{ steps.version.outputs.VERSION }}
        files: ./nupkg/*.nupkg
        generate_release_notes: true
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
